DATABASE_URL="file:./dev.db"
SESSION_SECRET="replace-with-a-long-random-string-at-least-32-chars"

# FastAPI backend (backend/)
MONGODB_URI="mongodb://localhost:27017"
MONGODB_DB="aussieeats"
JWT_SECRET="replace-with-a-long-random-string-at-least-32-chars"

# Google Maps JavaScript API key (Maps JS + Places libraries).
# Enables the interactive restaurant map and address autocomplete on /restaurants.
# Distance sorting and "Use my location" work without it. Exposed to the browser,
# so restrict the key by HTTP referrer / API in Google Cloud Console.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=""

# Server-only Places key for one-shot catalog ingest:
#   npm run db:import-places
# Enable the (legacy) Places API on the Google Cloud project. Falls back to
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY if unset. Prefer a separate key not restricted
# by HTTP referrer (IP restriction or unrestricted for local scripts).
GOOGLE_PLACES_API_KEY=""
